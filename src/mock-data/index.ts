import { mockOffersList } from './mock-offers-list';
import { mockFullOffer } from './mock-full-offer';
import { mockReviewsList } from './mock-reviews-list';
import { mockNearPlacesList } from './mock-near-places-list';

export { mockOffersList, mockFullOffer, mockReviewsList, mockNearPlacesList };
